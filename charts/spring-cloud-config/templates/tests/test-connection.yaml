apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spring-cloud-config.fullname" . }}-test-connection"
  labels:
{{ include "spring-cloud-config.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "spring-cloud-config.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
